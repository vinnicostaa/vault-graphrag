---
title: MOC — 03-subprojects/mobile
type: moc
tags: [moc, master-sindico, v2, subprojects, mobile, flutter, dart, bloc]
created: 2026-04-23
updated: 2026-04-23
---

# MOC — 03-subprojects/mobile

Sub-projeto **`app/`** — Mobile iOS + Android em **Flutter/Dart**, Clean Architecture + Feature First. Planos globais Stripe-style (D-057); Admin = `apps/admin` dedicada no monorepo web (D-134 Fase H revoga D-058/D-072 — mobile não tem telas admin, só ABAC).

Repo alvo pós-migração: `github.com/mastersindico/app`.

Stack canônica (confirmada): **Flutter + Dart 3.11+ + FVM + Clean Arch + flutter_bloc + get_it/injectable + go_router + Dio + flutter_secure_storage + oidc_pkce (Zitadel) + FCM + local_auth + device_info_plus + freezed (M1) + hive + bloc_concurrency + hydrated_bloc + freeRASP**.

> 🟢 **2026-04-27 (Agente 2)**: D-048 + D-049 deps adicionadas em `pubspec.yaml` — `bloc_concurrency ^0.3.0`, `hydrated_bloc ^10.1.1`, `freezed ^3.1.0`, `freezed_annotation ^3.1.0`, `json_serializable ^6.9.0`, `json_annotation ^4.9.0`, `path_provider ^2.1.5`. `fvm flutter pub get`/`analyze`/`test` ✅ (27 tests passed, No issues found). Adoção incremental por feature — refactor entities/blocs para usar `@freezed` + `concurrent`/`droppable` transformers fica para próximas sprints. **D-050** (jailbreak/root detection via freeRASP) ainda não aplicada — backlog. Detalhes: [[../../11-audit/audit-log/2026-04-27-agente-2-systematic-pass]].

---

## Arquivos

- [[README]] — visão geral; stack destilada do `pubspec.yaml` real + dívidas M1 explícitas (bloc_concurrency/hydrated_bloc/freezed/hive/local_auth/device_info_plus/firebase_messaging/app_links/freeRASP/easy_localization ausentes hoje); nativo mínimo (iOS 15, Android API 26, custom scheme `com.mastersindico`); módulos = BCs (auth + institutional + commercial + content + assembly + billing); canal distribuição (TestFlight + Play Console Internal → Prod; flavors dev/staging/prod via dart-define); estrutura canônica lib/app + lib/core + lib/features com ✅/🆕 por arquivo real vs alvo; contratos backend REST Bearer + X-Device-Fingerprint + X-Device-Integrity; OIDC PKCE Zitadel (ASWebAuthenticationSession iOS / Custom Tabs Android; nunca WebView); WebSocket assembleia + Livekit M3; push FCM payload canônico.

- [[architecture]] — Dependency Rule `presentation → domain ← data` (CODING_MANUAL §2.1) inviolável; camadas por feature com exemplo real `AuthRepositoryImpl` + Dart 3 records `({UserModel, String token})`; DI GetIt + Injectable (regra: `injection.config.dart` gerado, `SharedPreferences` registrado manual em main ANTES de `configureDependencies` — `throw UnimplementedError` intencional); router GoRouter top-level real com rotas assembleia `/condominiums/:id/assemblies/*` + alvo M1 ShellRoute com bottom nav + deep linking (universal links iOS + app links Android + custom scheme OIDC + allowlist + auth guard); state management flutter_bloc (D-048) + bloc_concurrency droppable + hydrated_bloc + migração para sealed classes freezed (D-049); `Either<Failure, T>` hierarquia real Server/Cache/Network/Auth/Unexpected + alvo M1 Forbidden/NewDevice/RateLimit/Validation; networking Dio interceptors Auth + Device + Refresh + Error + Log + cert pinning; offline-first seletivo (Hive + hydrated_bloc; boxes user/memberships/timeline/events/plano-diretor; writes online-only; sync queue tolerante; assembleia live online obrigatório); push FCM (handlers foreground/background/terminated + 6 categorias); video player Mux; upload presigned; theming com dívida reconciliação paleta Manual IV; observability Sentry + symbol upload; admin role (D-058) gated via ABAC na mesma app.

- [[patterns]] — Bloc patterns (event vs method; Equatable obrigatório; `part of`; navigation via BlocListener; BlocProvider local; bloc_concurrency.droppable() obrigatório em voto/Connect Me/pagamento; hydrated_bloc para memberships/timeline/plano-diretor); UseCase pattern real (`core/usecases/usecase.dart` com UseCase<T, Params> + NoParams — dívida: estender Equatable); factory/reconstruct DDD em Dart (factories `.create` / `.reconstruct`); value objects branded types wrapper (Email, CPF, CNPJ, FracaoIdeal, Money, UnitIdentifier, Score); first-class collections; Code Calisthenics adaptado (9 regras); copyWith manual vs freezed; platform channels evitar; state local setState apenas toggle UI; testing patterns (coverage thresholds domain 95 / bloc 90 / repo 85 / datasource 80 / pages 70 / widgets 80 / core 90 / app 60 / global ≥80; AAA + group + blocTest + widget tests + golden tests + mocktail sem codegen; integration tests M1+ auth/onboarding/vote/upload/sync); anti-patterns (token SharedPrefs=ERRADO, print em prod, http solto, validateStatus: (_)=>true, Color hardcode, BuildContext em Bloc, setState em business, Exception no domain, @LazySingleton em Bloc, WebView embed OIDC, deep-link sem allowlist, handler sem transformer em botão crítico, Entity com fromJson, @freezed em state mutável); convenções nomes (Bloc/Event/State/UseCase/Repository/Datasource/Model/VO); imports ordem (Dart → Flutter → third-party alfab → project relativo); comentários bilingues EN/PT obrigatórios; build_runner workflow + caveat freezed build time; commits Conventional Commits pt-BR.

- [[ui-catalog]] — §1 Sistema (splash, onboarding, biometria prompt, device mismatch, app outdated, integrity blocked D-050), §2 Auth (OIDC browser externo — implementado real), §3 Home shell bottom nav 4 abas, §4 Morador (painel, timeline, plano diretor, eventos, votação fornecedor M2, avaliação obrigatória, gerenciar acessos, onboarding wizard 4 steps — buscar condomínio/identificação unidade/termo ciência/status unidade), §5 Síndico mobile light M2 (governança/central/timeline voice-first/plano diretor/eventos/validações swipe actions/Connect Me/indicadores/modo apresentação), §6 Assembleia Live M3 (lista implementada /condominiums/:id/assemblies/*, detalhe implementado, ciência implementada, procuração M3, check-in QR M3, live session Livekit split vertical M3, voto item implementado com dívida droppable, ata implementada), §7 Connect Me morador M2, §8 Vídeo morador M2 (Mux Player + upload vídeo-currículo 90s + visibility temporário durante votação), §9 Perfil/conta/assinatura (home, perfil, assinatura + Stripe em Custom Tab, segurança + biometria + sessões, notificações 6 toggles, privacidade LGPD + biometria obrigatória delete, sobre), §10 Notificações (inbox + foreground toast + system), §11 Exceções (offline banner, 401 refresh, 403 NEW_DEVICE, 429 rate-limited, upload falhou, low battery/memory, outdated, integrity), §12 Admin role gated D-058 (bottom nav extra admin.access; admin home; bypass Lock 90d solicitar + aprovar 4-eyes biometria), §13 matriz paridade web M1-M3 + web-only (compliance/empresa/parceiro/agência/admin complexo/LP), §14 pendências dual-check P-APP-001..009.

- [[requirements]] — §1 Offline BR-MOB-OFF (Hive TTLs + sync queue + banner + writes online-only + assembleia live obrigatório) — D-041 Hive alvo; §2 Push BR-MOB-PUSH (FCM único + 4 tópicos + 6 channels/categories + payload canônico + PII restrito + tap deep-link); §3 Deep linking BR-MOB-DL (universal links iOS + app links Android + custom scheme OIDC + allowlist + auth guard preserve redirect); §4 Biometria BR-MOB-BIO (opt-in + local_auth + usos obrigatórios críticos + NÃO substitui OIDC + fallback); §5 Camera BR-MOB-CAM (permissões on-demand + 4 fluxos + compressão cliente + revoked handling); §6 Performance BR-MOB-PERF (cold start iOS 3s/Android 2.5s + 60fps + <200MB + <40MB IPA + battery); §7 i18n BR-MOB-I18N (pt-BR first + termos BR preservados + intl pt_BR + timezone D-052); §8 A11y BR-MOB-A11Y (Semantics + textScaler + 44×44/48×48 + 4.5:1 + reduce motion + TalkBack/VoiceOver); §9 Observability BR-MOB-OBS (Sentry 0.2 sample + symbol upload + logger + crash-free ≥99.5%); §10 Testing BR-MOB-QA (thresholds + gates + integration + device matrix Android 8/14 + iOS 15/17); §11 Paywall BR-MOB-BILL (frontend exibe backend decide D-057 + Stripe em Custom Tab, IAP não aplicável B2B); §12 LGPD BR-MOB-LGPD (consent + delete biometria + no PII log + clipboard); §13 Build BR-MOB-BUILD (flavors + dart-define + obfuscation); §14 Store BR-MOB-STORE (Privacy Labels + Data Safety + iOS 15/Android 26 + crash review); §15 resumo Security BR-MOB-SEC-001..010.

- [[security]] — §1 Bearer token em `flutter_secure_storage` (nunca SharedPreferences para tokens); estado real vs alvo M1 (keys canônicas + refresh_token persist); §2 certificate pinning prod + rotação semestral + dev sem pinning; §3 HTTPS-only ATS iOS + networkSecurityConfig Android; §4 OIDC PKCE via ASWebAuthenticationSession/Custom Tabs (nunca WebView) + config real OidcUserManager.lazy + custom scheme confirmado + state+nonce + scopes openid/profile/email/offline_access; §5 device fingerprint IR-011; §6 biometria local_auth usos obrigatórios (procuração/ata/delete/upgrade/bypass); §7 integrity D-050 escalonada (ok/dev-mode/jailbroken/hooked) + roll-out M1 report-only → M3 gated + freeRASP Talsec; §8 PII handling (logger wrapper + Sentry beforeSend scrubbing + clipboard + FLAG_SECURE screenshot); §9 secret management (dart-define obrigatório + sem secrets no mobile); §10 obfuscation + Sentry symbol upload; §11 dependency security (pub outdated semanal + Dio 5.8 CVE-fixed); §12 Hive encryption adicional com cipher em secure_storage + cache expiry; §13 session lifecycle (inatividade 30min + lock manual + backgrounded overlay + logout clean); §14 OWASP Mobile Top 10 controles M1-M10; §15 incident response device comprometido; §16 admin D-058 considerações mobile (4-eyes bypass Lock 90d com biometria); §17 checklist pré-release store.

- [[tasks]] — DT-MB-001..013 dívidas atuais (minSdk 19 → 26, bundle ID normalizar, 10 deps ausentes em pubspec, NoParams não-Equatable, paleta placeholder, keys inconsistentes, refresh token não persiste, Info.plist sem permissões, AndroidManifest sem network_config/app-links, flavors ausentes, rotas assembleia fora do shell); Sprint 10 M1 (setup/infra 13 tasks + auth 8 + core DS 7 + home shell 9 + onboarding 6 + conta 6 + offline Hive 5 + push FCM 7 + testing 7 + security 7 + build/store 4); Sprint 11 M2 (síndico light 10 + Connect Me morador 4 + vídeo morador 3 + votação fornecedor 2 + voice input 2 + camera 2 + offline sync queue 2 + admin D-058 3 + testing 4); Sprint 12 M3 (assembleia live 7 + LMS thin 4 + apresentação 1 + security D-050 gated 2 + polish 5 + store submit 3 + testing 4); backlog pós-M3 (empresa app + widgets + wearOS + offline pleno + rich push + a11y audit + feature flags + A/B + EN i18n + baixo-consumo); tasks transversais (fastlane CI/CD + crashlytics opcional + Firebase Performance + pre-commit + build_runner watch); gates de merge DoD.

---

## Vizinhos

- [[../../_moc]] — MOC 03-subprojects (backend + web + mobile)
- [[../../CLAUDE]] — contrato do agente
- [[../../STEERING]] — não-negociáveis
- [[../../STATE]] — decisões vivas (D-###)
- [[../../AUDIT]] — findings abertos (A-###)
- [[../../ROADMAP]] — M1/M2/M3
- [[../backend/README]] — contrato server-side consumido
- [[../web/design-system]] — design tokens espelhados (com dívida reconciliação)
- [[../web/ui-catalog]] — source de jornadas (mobile é subset)
- [[../../04-requirements/_moc]]
- [[../../08-security/BEYOND_CORP]]
- [[../../STATE#D-048|D-048 Bloc + bloc_concurrency + hydrated_bloc]]
- [[../../STATE#D-049|D-049 freezed M1]]
- [[../../STATE#D-050|D-050 Integrity escalonada MASVS-R]]
- [[../../STATE#D-057|D-057 Planos globais Stripe-style]]
- [[../../STATE#D-134|D-134 Admin = `apps/admin` dedicada (revoga D-058/D-072)]]
